Part-built prop models (`.model.json`) generated by `tools/gen_props.py`. These ship inside the
place file so the game is playable with zero manual asset uploads. Blender-exported meshes in
`assets/export/` are the optional high-fidelity replacements; see `assets/blender/README.md`.
